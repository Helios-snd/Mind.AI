# Copy to .env.local

# Presence of this switches src/api/hooks.ts from mockClient to httpClient.
# Unset it to run the whole app offline against the mock.
NEXT_PUBLIC_API_URL=/api/v1

# Where next.config.mjs proxies /api/v1/* to.
BACKEND_ORIGIN=http://localhost:8000
